# static-routing
Built a static routing network using Cisco Packet Tracer with 3 routers, 3 switches, and 6 PCs across different subnets. Configured serial and LAN interfaces, assigned IPs, and enabled inter-network communication. Verified connectivity through successful end-to-end ping tests.
